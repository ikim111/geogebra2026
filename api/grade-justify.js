// Vercel 서버리스 함수: teacherpa1.html의 "AI로 이유·방법 채점" 버튼이 호출하는 엔드포인트.
// Gemini API 키를 브라우저에 노출하지 않기 위해, 키는 이 서버 함수 안(Vercel 환경변수)에만
// 두고, 채점 요청/응답만 브라우저와 주고받는다. (Flash-Lite는 Google AI Studio 무료
// 티어로도 충분히 쓸 수 있어서, 이 단순한 채점 용도에는 비용이 거의/전혀 들지 않는다.)
//
// [설정 방법] Vercel 프로젝트 설정 > Settings > Environment Variables 에서
//   이름: GEMINI_API_KEY
//   값:   Google AI Studio(aistudio.google.com/apikey)에서 발급한 API 키
// 를 등록하고 다시 배포(재배포)하면 이 함수가 그 키를 쓸 수 있다.
//
// 요청 형식(POST, JSON): { centerType: '외심'|'내심', reason: string, method: string }
// 응답 형식(JSON): { reasonCorrect, reasonFeedback, methodPart1Correct, methodPart2Correct,
//                    methodPart3Correct, methodFeedback }
// - reasonCorrect: ②이유 문항 정답 여부(2점, 전부-아니면-0점)
// - methodPart1/2/3Correct: ③방법 문항을 세 요소로 나눠 채점(각 1점, 부분점수 가능).
//   외심이면 [변]/[수직이등분선]/[교점], 내심이면 [각]/[이등분선]/[교점] 순서.

const GEMINI_MODEL = 'gemini-3.5-flash-lite';

module.exports = async (req, res) => {
  if (req.method !== 'POST') {
    res.status(405).json({ error: 'POST 요청만 지원합니다.' });
    return;
  }

  const apiKey = process.env.GEMINI_API_KEY;
  if (!apiKey) {
    res.status(500).json({ error: 'GEMINI_API_KEY가 설정되지 않았습니다. Vercel 프로젝트 환경변수를 확인해주세요.' });
    return;
  }

  let body = req.body;
  if (typeof body === 'string') {
    try { body = JSON.parse(body); } catch (e) { body = {}; }
  }
  const centerType = (body && body.centerType) === '내심' ? '내심' : '외심';
  const reason = (body && body.reason) || '';
  const method = (body && body.method) || '';

  if (!reason.trim() && !method.trim()) {
    res.status(400).json({ error: '채점할 이유/방법 답안이 비어 있습니다.' });
    return;
  }

  // [과제2] 외심 채점 기준: (2) 세 점(꼭짓점)으로부터 거리가 같아야 하기 때문이다. (3) 세 변의
  // 수직이등분선의 교점을 그린다 → [변]/[수직이등분선]/[교점] 세 요소가 모두 들어가야 만점.
  // [과제4] 내심 채점 기준: (2) 세 변으로부터 거리가 같아야 하기 때문이다. (3) 세 각의
  // 이등분선의 교점을 그린다 → [각]/[이등분선]/[교점] 세 요소가 모두 들어가야 만점.
  const expectedReason = centerType === '내심'
    ? '세 변으로부터 거리가 같아야 하기 때문이다.'
    : '세 점(꼭짓점)으로부터 거리가 같아야 하기 때문이다.';
  const methodParts = centerType === '내심'
    ? ['각(세 내각)을 이등분한다는 내용', '(각의) 이등분선이라는 내용', '그 이등분선들의 교점을 찾는다는 내용']
    : ['변(세 변)을 이등분한다는 내용', '수직이등분선이라는 내용(변에 수직으로 이등분)', '그 수직이등분선들의 교점을 찾는다는 내용'];
  const expectedMethod = centerType === '내심'
    ? '세 각의 이등분선의 교점을 그린다.'
    : '세 변의 수직이등분선의 교점을 그린다.';

  // ↓ 아래 [이유 채점 시 특히 주의] / [방법 채점 시 특히 주의] 두 단락은, 선생님이 실제 학생 답안에서
  // AI 채점 결과를 여러 건 직접 수정(정정)한 사례들을 분석해서 반영한 것이다(2026-09-19).
  // 1) "세 지점/관측소에서 거리가 같다"(=꼭짓점 기준, 외심의 성질)와 "세 변/도로에서 거리가 같다"
  //    (=변 기준, 내심의 성질)을 AI가 서로 헷갈려서, 내심 문제인데 학생이 꼭짓점 기준으로 설명한
  //    답(반대의 경우도 마찬가지)을 "거리가 같다"는 표현만 보고 정답으로 잘못 인정하는 경우가
  //    여러 번 있었다 — 선생님이 오답으로 정정.
  // 2) 문제 시나리오에 따라 "점"이 "관측소/지점/꼭짓점" 등으로, "변"이 "도로" 등으로 다르게
  //    표현될 수 있는데("도로"=변), AI가 이런 시나리오 어휘를 변/점 개념과 잘못 연결해 오답으로
  //    처리한 경우도 있었다 — 선생님이 정답으로 정정.
  // 3) 방법 문항에서, 1·2번 요소(선의 종류: 수직이등분선/각의 이등분선)의 용어를 학생이 반대
  //    센터의 것과 섞어 썼더라도, "그 선들이 만나는 지점을 찾는다"는 3번 요소(교점) 자체는
  //    독립적으로 명확히 서술되어 있으면 인정해야 하는데, AI가 1·2번의 오류를 이유로 3번까지
  //    같이 오답 처리한 경우가 있었다 — 선생님이 3번만 정답으로 정정.
  // (2026-09-20, 6반 추가 정정 사례 반영)
  // 4) "세 각 이등분선은 내심에서 만난다"처럼, 이유 문항에 "방법/구성 사실"(무엇이 어디서 만난다)만
  //    쓰고 "왜 그 조건(거리가 같음)이 그 점을 정의하는지"는 답하지 않은 경우를, AI가 주제가
  //    비슷하다는 이유로 정답 처리한 사례가 있었다 — 선생님이 오답으로 정정.
  // 5) 3번 요소(교점)를 "교점을 찾고"처럼 정확히 어떤 선인지는 불명확해도 "교점"이라는 개념 자체는
  //    명시한 답은 AI가 오답 처리했다가 선생님이 정답으로 정정한 경우와, 반대로 "그 점을
  //    OO(이)라 하고"처럼 교점을 찾는 과정 자체를 언급하지 않고 결과만 서술한 답을 AI가 정답
  //    처리했다가 선생님이 오답으로 정정한 경우가 둘 다 있었다 — "교점/만나는 지점을 찾는다"는
  //    개념이 (다소 막연하더라도) 명시적으로 등장하는지가 기준이다.
  // (2026-09-20, 5·6반 2차 재채점 검토 후 반영)
  // 6) 이유 문항에서 "외심을 찾아야 세 점(위치)으로부터 그 점까지의 거리를 같게 할 수 있다"처럼
  //    목적/조건절("~해야 ~할 수 있다") 형태로 쓴 답을, AI가 "문제 조건을 되풀이했을 뿐"이라거나
  //    "순환 논리"라고 보고 오답 처리한 사례가 있었는데, 선생님이 검토 결과 이는 기준(세 점)과
  //    결론(거리가 같다)이 정확히 짝지어 서술된 정상적인 설명이라고 판단해 정답으로 정정했다.
  // 7) 방법 문항 3번 요소(교점)에서, 답안 앞부분에 "~의 교점(외심)을 구해야 한다"처럼 교점이라는
  //    단어가 목표를 설명하는 문장에 등장했는데도, 뒤이은 과정 설명이 다소 모호하다는 이유로
  //    AI가 3번 요소를 오답 처리한 사례를 선생님이 정답으로 정정했다 — "교점"이라는 단어가 답안
  //    어디에(목표 설명이든 과정 설명이든) 등장하기만 하면 인정해야 한다는 취지.
  const prompt = `당신은 중학교 수학(삼각형의 외심과 내심) 수행평가 서술형 답안을 채점하는 선생님입니다.
학생이 이번 문제에서 찾아야 하는 점: ${centerType}

[이유 문항] 모범 답안: "${expectedReason}" — 표현이 다르더라도 핵심 개념(무엇으로부터 거리가 같은지)이 통하면 정답(true)으로 판정하세요.

[이유 채점 시 특히 주의] "거리가 같다"는 표현이 있다고 무조건 정답으로 인정하지 말고, 그 거리가 "무엇으로부터"인지(기준)를 반드시 확인하세요.
  - 외심(circumcenter)의 기준은 "세 점"(꼭짓점) — 문제 시나리오에 따라 "관측소", "지점" 등으로 표현될 수 있습니다.
  - 내심(incenter)의 기준은 "세 변" — 문제 시나리오에 따라 "도로" 등으로 표현될 수 있습니다(예: "도로에서 거리가 같다" = "변에서 거리가 같다"와 같은 뜻이므로 내심 문제에서 정답으로 인정).
  - 지금 채점 대상은 "${centerType}"이므로 기준은 "${centerType==='내심'?'변(도로 등)':'점(꼭짓점, 관측소, 지점 등)'}"이어야 합니다. 학생 답이 "세 지점/꼭짓점에서 거리가 같다"처럼 반대 기준(${centerType==='내심'?'점':'변'} 기준)을 설명하고 있다면, "거리가 같다"라는 단어가 등장하더라도 오답(false)입니다.
  - 답이 "~에 있는 세 거리가 같아지려면" 처럼 기준이 무엇인지 모호하거나 빠져 있으면, 핵심 개념이 실제로 전달되지 않은 것이므로 오답(false)으로 처리하세요.
  - 이유 문항은 "왜 그 점(거리가 같다는 조건)이 성립하는지"를 답해야 합니다. "세 이등분선은 ○○에서 만난다"처럼 무엇이 어디서 만나는지(=방법/구성 사실)만 쓰고 "거리가 같다"는 조건 자체를 전혀 언급하지 않은 답은, 주제가 비슷해 보여도 이유 문항의 정답으로 인정하지 마세요(방법 문항에서 다룰 내용을 이유 문항 자리에 쓴 것입니다) — 오답(false)으로 처리하세요.
  - 단, 학생이 "세 점(또는 변)으로부터 거리가 같다"는 결론에 실제로 도달하기만 했다면, 그 결론에 이르는 설명 방식(예: "모든 점이 같은 원 위에 있으므로 반지름이 같다", "외접원/내접원을 그려보면 세 점을 모두 지나므로 거리가 같음이 증명된다" 등 원을 이용한 증명)은 정답으로 인정하세요. 교과서 문장을 토씨 하나 안 틀리고 써야만 정답인 것이 아닙니다 — 핵심 결론(거리가 같다 + 그 기준이 무엇인지)이 답안 어딘가에 명확히 드러나 있다면, 설명이 다소 길거나 증명 방식을 덧붙였다고 해서 감점하지 마세요. "거리가 같다"는 결론에 아예 도달하지 못했거나(예: 그냥 문제 조건을 되풀이하기만 함), 그 결론의 기준(점/변)이 틀리거나 불명확한 경우에만 오답으로 처리하세요.
  - "~해야 ~할 수 있다", "~하려면 ~해야 한다"처럼 목적/조건을 나타내는 문장 구조라도, 그 안에 정확한 기준(${centerType==='내심'?'변':'점'})과 "거리가 같다"는 결론이 함께 명확히 들어있다면, 이는 ${centerType}의 정의적 성질을 정확히 설명한 것이므로 정답(true)으로 인정하세요. 예를 들어 "외심을 찾아야 세 점(위치)으로부터 그 점까지의 거리를 같게 할 수 있다"는 기준(세 점)과 결론(거리가 같다)이 모두 정확히 짝지어 있으므로 정답입니다. 이런 문장을 단지 조건문 형태라는 이유만으로 "문제 조건을 되풀이했을 뿐"이라거나 "순환 논리"라고 판단해 오답 처리하지 마세요 — 기준과 결론이 정확히 짝지어 서술되어 있다면 그 자체로 충분한 설명입니다. (반대로 기준이나 결론 중 하나라도 빠지거나 틀린 채로 막연히 "찾아야 한다"고만 쓴 경우는 여전히 오답입니다.)

[방법 문항] 모범 답안: "${expectedMethod}" — 이 문항은 아래 세 가지 요소가 각각 들어있는지 따로따로 채점합니다(부분점수 가능). 표현이 다르더라도 의미가 통하면 정답으로 인정하세요.
  1번 요소(part1): ${methodParts[0]}
  2번 요소(part2): ${methodParts[1]}
  3번 요소(part3): ${methodParts[2]}
주의: 답안이 완전히 다른 개념(예: ${centerType==='내심' ? '수직이등분선/외심' : '각의 이등분선/내심'} 관련 설명)을 쓴 경우, 그 요소들은 겉으로 비슷한 단어가 있더라도 정답으로 인정하지 마세요(예: "변의 수직이등분선"이라는 답은 [각]/[이등분선(각의)] 요소를 만족하지 않습니다).
[방법 채점 시 특히 주의] 3번 요소(교점)는 1·2번과 독립적으로 채점하세요. 1·2번에서 선의 종류(수직이등분선/각의 이등분선) 용어를 반대 센터의 것과 섞어 썼거나 틀렸더라도, "그렇게 그은 선(또는 이등분선)들이 만나는 지점을 찾는다"는 내용이 명확히 있으면 3번은 정답(true)으로 인정하세요 — 1·2번이 틀렸다는 이유만으로 3번까지 자동으로 오답 처리하지 마세요.
3번 요소(교점) 판정 기준은 "교점" 또는 "만나는 지점"에 해당하는 단어(또는 명백히 같은 뜻)가 답안 어디엔가(목표를 설명하는 문장이든, 과정을 설명하는 문장이든 위치는 상관없습니다) 명시적으로 등장하는지입니다. "교점을 찾고"처럼 정확히 어떤 선들의 교점인지 다소 불명확해도, 또는 "~의 교점(을) 구해야 한다"처럼 목표를 밝히는 문장에서만 언급되고 그 뒤 과정 설명이 다소 모호하더라도, "교점"이라는 단어 자체가 답안에 나왔다면 정답(true)으로 인정하세요 — 뒤이은 설명이 애매하다는 이유로 이미 명시된 "교점" 언급을 무효로 취급하지 마세요. 3번 요소가 오답(false)이 되는 경우는 오직, "그 점을 ○○(이)라 하고"처럼 "교점"/"만나는 지점"이라는 단어 자체가 답안 전체에 한 번도 등장하지 않고 결과(점의 이름)만 이야기하는 경우뿐입니다.
1번 요소(${centerType==='내심'?'각':'변'})는 "${centerType==='내심'?'각':'변'}"이라는 단어(또는 명백히 같은 뜻: 외심이면 "세 변"/"변 AB, BC, CA" 등, 내심이면 "세 각"/"내각" 등)이 답안에 실제로 등장해야 정답(true)입니다. "수직이등분선"이나 "이등분선"이라는 단어에 그 대상(변 또는 각)이 이미 내포되어 있다고 넘겨짚어 자동으로 정답 처리하지 마세요 — "수직이등분선의 교점을 찾는다"처럼 1번 요소에 해당하는 단어 없이 2·3번만 쓴 답은 1번 요소를 오답(false)으로 채점해야 합니다(이 경우 방법 문항 점수는 3점 만점에 2점이 됩니다).
${centerType!=='내심' ? `  (2026-09-20, 선생님 정정 반영) 외심 문제에서 "변" 대신 "선분"이라는 단어를 쓴 경우(예: "선분 AB, BC, CA의 수직이등분선", "세 점을 선분으로 이어서 그 선분에 수직이등분선을 긋는다")는 1번 요소를 정답(true)으로 인정하세요 — 삼각형의 변은 두 꼭짓점을 잇는 선분과 같은 대상이므로, "선분"이라는 단어가 명시적으로 등장했다면 "변"을 쓴 것과 동일하게 취급합니다. 단, "선분"이라는 단어 자체도 전혀 없이 그냥 "점 사이에 수직이등분선을 그으면"이나 "수직이등분선의 교점을 찾으면"처럼 점/선을 막연히 언급한 경우는 "변"도 "선분"도 등장하지 않은 것이므로 여전히 1번 요소를 오답(false)으로 처리하세요. 즉 1번 요소가 정답이 되려면 "변" 또는 "선분" 둘 중 하나의 단어가 답안에 실제로 등장해야 합니다.` : ''}
2번 요소(${centerType==='내심'?'이등분선':'수직이등분선'})는 "${methodParts[1]}"에 정확히 해당하는 선을 말했을 때만 정답입니다. ${centerType==='내심'
  ? `내심 문제의 2번 요소는 "(각의) 이등분선"입니다. "수직이등분선"이라는 단어가 등장하면 — 설령 "각의 수직이등분선"처럼 1번 요소(각)와 함께 쓰였더라도 — 2번 요소는 오답(false)입니다. "수직이등분선"은 글자 중에 "이등분선"을 포함할 뿐, 실제로는 변에 수직으로 긋는 전혀 다른 선(외심에서 쓰는 선)이므로, 글자가 일부 겹친다고 정답 처리하면 안 됩니다. 이때 1번 요소(각)는 "각"이라는 단어가 등장했다면 그대로 정답으로 유지하세요 — 즉 "각의 수직이등분선"이라고 쓴 답은 1번만 정답(1점), 2번은 오답(0점)으로 부분점수만 주어야 합니다.`
  : `외심 문제의 2번 요소는 "수직이등분선"입니다. 단순히 "이등분선"이라고만 쓰고 "수직"이라는 표현이 전혀 없다면("각의 이등분선"처럼 다른 선을 가리키는 것이 아니라, 그냥 "변을 이등분한다"는 정도로만 쓴 경우) 2번 요소는 오답(false)입니다 — 변을 반으로 나누는 것과, 그 지점에서 변에 수직으로 선을 긋는 것은 다른 개념입니다.`}

학생 답안:
② 이유: "${reason || '(작성 안 함)'}"
③ 방법: "${method || '(작성 안 함)'}"

각 문항에 대해 학생에게 보여줄 짧은 한국어 피드백도 한 줄씩 작성하세요(방법 문항은 세 요소 중 무엇이 부족한지 짚어주세요).`;

  try {
    const url = `https://generativelanguage.googleapis.com/v1beta/models/${GEMINI_MODEL}:generateContent`;
    const r = await fetch(url, {
      method: 'POST',
      headers: {
        'content-type': 'application/json',
        'x-goog-api-key': apiKey
      },
      body: JSON.stringify({
        contents: [{ parts: [{ text: prompt }] }],
        generationConfig: {
          // temperature 0: 같은 답안 텍스트는 최대한 같은 채점 결과가 나오도록(완전한 결정성은
          // 보장되지 않지만 0.2보다 훨씬 일관적이다). 수행평가 채점은 "같은 답이면 같은 점수"가
          // 지켜져야 하므로(2026-09-20, 선생님 피드백) 정확도(다양한 표현 이해)보다 일관성을
          // 우선한다. 완전한 일관성은 teacherpa1.html의 답안 텍스트 기반 채점 결과 캐시로 보장한다.
          temperature: 0,
          responseMimeType: 'application/json',
          responseSchema: {
            type: 'object',
            properties: {
              reasonCorrect: { type: 'boolean' },
              reasonFeedback: { type: 'string' },
              methodPart1Correct: { type: 'boolean' },
              methodPart2Correct: { type: 'boolean' },
              methodPart3Correct: { type: 'boolean' },
              methodFeedback: { type: 'string' }
            },
            required: ['reasonCorrect', 'reasonFeedback', 'methodPart1Correct', 'methodPart2Correct', 'methodPart3Correct', 'methodFeedback']
          }
        }
      })
    });

    if (!r.ok) {
      const errText = await r.text();
      res.status(502).json({ error: 'Gemini API 호출 실패', detail: errText });
      return;
    }

    const data = await r.json();
    const text = data && data.candidates && data.candidates[0] &&
      data.candidates[0].content && data.candidates[0].content.parts &&
      data.candidates[0].content.parts[0] && data.candidates[0].content.parts[0].text;

    if (!text) {
      res.status(502).json({ error: 'Gemini 응답에 채점 결과가 없습니다.', raw: data });
      return;
    }

    let parsed = null;
    try {
      parsed = JSON.parse(text);
    } catch (e) {
      res.status(502).json({ error: 'AI 응답을 해석하지 못했습니다.', raw: text });
      return;
    }

    res.status(200).json({
      reasonCorrect: !!parsed.reasonCorrect,
      reasonFeedback: parsed.reasonFeedback || '',
      methodPart1Correct: !!parsed.methodPart1Correct,
      methodPart2Correct: !!parsed.methodPart2Correct,
      methodPart3Correct: !!parsed.methodPart3Correct,
      methodFeedback: parsed.methodFeedback || ''
    });
  } catch (err) {
    res.status(500).json({ error: err.message || String(err) });
  }
};
